import React from 'react';
import ReactDOM from 'react-dom';
import Index from './random/index';

ReactDOM.render(
    <Index/>,
    document.getElementById('root')

)
if(module.hot){
    module.hot.accept();
  }
